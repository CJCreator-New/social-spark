# Implementation Plan — Social Spark

## Overview
This document captures the prioritized implementation backlog, targeted fixes for the top-5 pages, and the visual/UX redesign pass for highest-traffic pages (Index and CalendarDetail).

### Prioritized Backlog
- Quick Wins
  - Stabilize prompt framework — tests green
  - Fix clipboard/hook identity in `src/pages/Index.tsx`
  - Guard optional external imports (`src/lib/monitoring.ts`, `src/lib/metrics.ts`)
  - Tighten adapter types (`supabase/functions/adapters/*.ts`)

- Medium
  - Fix Profile loading edge cases
  - Persist schedule timezone & draft saving
  - Accessibility fixes for critical flows

- Large
  - Decompose Index & CalendarDetail monoliths
  - Admin telemetry wiring & feature flags

### Top 5 Page Fix Plan
1. Index: split into `IndexHeader`, `IndexFeed`, `IndexControls`. Add `useIndexData` hook. Acceptance: no regressions, improved render stability.
2. CalendarDetail: extract `WeekGrid`, `EventCard`. Add keyboard navigation. Acceptance: keyboard + screen reader passes.
3. Profile: guard missing data and add empty states.
4. Schedule: persist timezone and robust draft save/restore.
5. Landing/Auth/NotFound: meta tags and form accessibility fixes.

### Visual / UX Redesign (Index & CalendarDetail)
- Tokens: `src/styles/tokens.css` with color/spacing/typography variables
- Component patterns: `Card`, `Button`, `Pill`, `Skeleton`
- Two-panel responsive layout for Index; compact/dense toggle for CalendarDetail

### Sprint Plan (2-week)
- Week 1: Quick Wins + tokens + component scaffolding + targeted fixes for Index and Profile
- Week 2: CalendarDetail refactor + Schedule persistence + accessibility + E2E checks

### Acceptance Criteria
- Typecheck: `npm run typecheck` passes
- Lint: `npm run lint` passes
- Tests: `npm run test:run` passes (existing tests) and new targeted tests added
- Accessibility: core flows pass automated Axe checks

---
Generated by the app enhancement workflow. Implementations are incremental and covered by CI checks.
